/* --------------------------------------------------------------------------------
  This file was automatically generated by the Athlos Project Editor.
  Athlos Project Editor, v1.0 BETA
-------------------------------------------------------------------------------- */

package com.example.marspioneer.client.websocket;

import com.example.marspioneer.client.MPClient;
import com.example.marspioneer.proto.BuildResponse;
import com.google.protobuf.InvalidProtocolBufferException;
import com.neovisionaries.ws.client.WebSocketException;
import com.raylabz.mocha.binary.client.BinaryWebSocketClient;

import java.io.IOException;

public class BuildMineStub extends BinaryWebSocketClient {

    private MPClient client;

    public BuildMineStub(MPClient client) throws IOException, WebSocketException {
        super("ws://localhost:8080/api/action/buildMine");
        this.client = client;
    }

    @Override
    public void onReceive(byte[] bytes) {
        try {
            BuildResponse response = BuildResponse.parseFrom(bytes);
            handleResponse(response);
        } catch (InvalidProtocolBufferException e) {
            e.printStackTrace();
            client.getGameCanvas().showMessage(e.getMessage());
        }
    }
    
    public void handleResponse(BuildResponse response) {
        if (response.getStatus() == BuildResponse.Status.OK) {
            System.out.println("Mine built by " + client.getWorldSession().getId());
        } else {
            client.getGameCanvas().showMessage(response.getMessage());
            System.err.println(response.getMessage());
        }
    }

}

